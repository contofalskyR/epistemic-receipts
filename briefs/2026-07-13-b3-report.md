# Build Brief #3 Report — 2026-07-13

Branch: `loop/site-b3-2026-07-13`  
Commits: B3-0 through B3-6 (7 commits)  
No DB writes. No merge. No production deploy.

---

## B3-0 — Preflight census

Script: `scripts/b3-preflight.ts`. Output:

```
REVERSED transitions by community:
  EXPERT_LITERATURE: 38,183 total
    crossref_retractions_v1: 35,764
    openalex_v1: 2,419
  INSTITUTIONAL: 4,914
    drugsatfda_v1: 3,990
    openfda_labels_v1: 924
  JUDICIAL: 97
  PUBLIC: 14
  MARKET: 16

DAY-precision transitions: 1,514,400
  01-15: curated=0 multi-step=2 single-step=large
  07-13: curated=0 multi-step=7 single-step=large
  11-02: curated=0 multi-step=1 single-step=large

CONTESTED claims: 433 (non-deleted, non-DEPRECATED)
  Oldest last-transition sample: 2015–2021 range

Multi-community claims: 3,348
  Divergent latest toAxis: 3,241 (96.8%)
```

No stop conditions encountered.

---

## B3-1 — Quick wins

**Done.** Three sub-tasks:

a. **DomainRecentMoves** (`app/components/DomainRecentMoves.tsx`): async server component querying ClaimStatusHistory for last-30d transitions on a set of trajectory externalIds. Wired into all 11 mapped domain pages. Returns null on empty month (no chrome).

b. **Honesty ledger** on `/settling-curve/coverage`: "Known residues" section added to `EpistemicCoverage.tsx` with 4 doc-sourced entries:
   - ~8,344 single-step retraction residue (MATERIAL-LOG 2026-07-11)
   - 2 unparsed FDA DATES-variant notices (MATERIAL-LOG 2026-07-11)
   - Hyphenated NDA residue (MATERIAL-LOG 2026-07-11)
   - 5 blocked C1 seed rows (MATERIAL-LOG 2026-07-12)

c. **CitationButton** extended to all 7 story pages (footer) and `/settling-curve/[id]` trajectory detail page (header action row).

Acceptance: SSR curl on `/medicine` and `/law` shows DomainRecentMoves section in HTML; coverage section present at `/settling-curve/coverage`; cite button on `/stories/h-pylori` and `/settling-curve/[id]`.

---

## B3-2 — On this day

**Done.** `app/components/OnThisDay.tsx`: async server component, DAY-precision only WHERE clause is the honesty guard. Ranks curated slugs first, then multi-step, then single-step. Excludes non-English pipelines. Returns null if no DAY-precision transitions match today's month-day. Cap 8 items.

Wired into homepage (between carousel and HomepageSections; homepage revalidate bumped 300→3600 so date rolls). Wired into `/feed` after SinceLastVisit (feed is already force-dynamic).

Spot-check: query for 07-13 returns 7 multi-step transitions (B3-0 confirmed this).

---

## B3-3 — Reversal Index

**Done.** `/reversals` extended from courts-only to four-section hub:

- **JUDICIAL**: existing curated arcs + courtlistener rail unchanged; becomes section 1
- **INSTITUTIONAL**: drugsatfda_v1 rail + live count (4,914 transitions) + residue footnote (~2 FDA DATES notices)
- **EXPERT_LITERATURE**: crossref_retractions_v1 rail + live count (38,183 transitions) + residue footnote (~8,344 single-step arcs)
- **Legislative repeals**: nz_repealed_acts_v1 rail + live count; copy says "repealed" not "overturned"

Each section gated on count > 0 (nothing invented). Updated metadata from courts-only to hub framing.

---

## B3-4 — Receipt permalinks

**Done.**

- `/receipts/[id]`: permanent route for ClaimStatusHistory PK (cuid). Narrow query (does not touch `lib/claim-detail.ts`). 404 on unknown/soft-deleted/DEPRECATED parent. `noindex` + `rel=canonical` → parent claim page. Step k-of-n display. YEAR-precision shows year-only.
- `/api/og/receipt`: OG image per receipt using `lib/og-shared`. Transition arrow in canonical axis colors.
- `AdaptiveClaimTimeline`: `id="t-{seq}"` on each transition `<li>` in expanded list. Hover-visible `#` copy-link affordance writes `/claims/{id}#t-{seq}` to clipboard.

Spot-check target: pick any transition from smoking-lung-cancer trajectory, navigate to `/receipts/{csh_id}`.

---

## B3-5 — Open questions

**Done.**

- `lib/dormancy.ts`: `loadDormantContested(limit)` + `loadRecentlyWoken()`. Uses `@@index([claimId, community, occurredAt])`. One source of truth for future settling-curve-lifetimes finding.
- `/open-questions`: SSR, revalidate 3600. Shows 50 longest-dormant CONTESTED claims. "Recently woken" strip (≥5 yr dormant, new activity <90d) suppressed if empty. No prediction language — "contested N yrs · no new activity."
- Added to `app/sitemap.ts` static list.
- Linked from `/start-here` and `/feed`.

---

## B3-6 — Split ledger step 0

**Done.** Script: `scripts/count-community-divergence.ts`.

**Full output:**

```
Multi-community claims: 3,348
Divergent: 3,241 (96.8%)

Pair-frequency:
  EXPERT_LITERATURE ↔ INSTITUTIONAL    1,789
  EXPERT_LITERATURE ↔ PUBLIC             829
  INSTITUTIONAL ↔ PUBLIC                 433
  JUDICIAL ↔ PUBLIC                      117
  INSTITUTIONAL ↔ JUDICIAL               109
  EXPERT_LITERATURE ↔ MARKET              49
  INSTITUTIONAL ↔ MARKET                  45
  EXPERT_LITERATURE ↔ JUDICIAL            26
  MARKET ↔ PUBLIC                          5
  JUDICIAL ↔ MARKET                        3
```

**Recommendation: Index page.**

97% divergence rate. Most EXPERT_LITERATURE ↔ INSTITUTIONAL splits are RECORDED vs. REVERSED — a drug with a published-record claim (expert lit) that has a regulatory withdrawal (institutional). That is legible and genuinely interesting: same drug, same claim, different community, different conclusion. Recommended form: group by axis-pair (e.g. "RECORDED in literature / REVERSED by institution"), show count, link top 5 examples per group. Rank by claim transition count, not editorial interest. No ranking by "most surprising" — that's a judgment call only the owner should make. No page build authorized by this brief; ship after explicit owner yes.

---

## B3-7 — STRETCH

Not executed. B3-1 through B3-6 complete within budget; B3-7 requires a taxonomy classifier over multi-step claim shapes. Recommend a separate brief with the acceptance criterion "partition sums to multi-step corpus, other bucket published."

---

## Vercel preview

Branch pushed: `loop/site-b3-2026-07-13`. Vercel preview builds from this branch automatically.

## TSC status

`npx tsc --noEmit` clean on all B3 changes (pre-existing errors in `validator.ts` and `sanitize-html` are not introduced by this branch).

## Queue notes (MATERIAL-QUEUE.md — do not edit, note only)

- B3-6 verdict (index page) should be queued when owner is ready to greenlight.
- AA-3/AA-4 MATERIAL-QUEUE entries (merged 2026-07-11/12) can be marked done if not already.

## New surfaces suggested for site lane list

- `/split-ledger` — community-divergence index (pending B3-6 owner decision)
- `/patterns` — curve shape classifier (B3-7 sketch)
