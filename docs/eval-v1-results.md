# Eval v1 Results — er-eval-v1

## Status

**BLOCKED on live DB run.** The builder script (`scripts/build-eval-set.ts`)
requires `DATABASE_URL` in `.env.local`. This environment has no DB access.
Construction stats and baseline scores below are _template entries_ — replace
with actual output after running against production.

---

## Construction stats

_Run command:_
```bash
npx tsx scripts/build-eval-set.ts --free --seed 42 --snapshot <YYYYMMDD> 2>&1 | tee build-stats-free.txt
npx tsx scripts/build-eval-set.ts --full --seed 42 --snapshot <YYYYMMDD> 2>&1 | tee build-stats-full.txt
```

| Stat | Free (500) | Full (5,000) |
|------|-----------|--------------|
| Eligible transitions fetched | BLOCKED | BLOCKED |
| Claims with ≥2 transitions | BLOCKED | BLOCKED |
| as_of_status items (before dedup) | BLOCKED | BLOCKED |
| transition_attribution items | BLOCKED | BLOCKED |
| reversal_awareness items | BLOCKED | BLOCKED |
| Leakage-flagged (excluded) | BLOCKED | BLOCKED |
| Ambiguity-excluded | BLOCKED | BLOCKED |
| Negative controls generated | BLOCKED | BLOCKED |
| Negative-control collision-rejected | BLOCKED | BLOCKED |
| **Final item count** | BLOCKED | BLOCKED |
| SHA-256 (run 1) | BLOCKED | BLOCKED |
| SHA-256 (run 2, determinism check) | BLOCKED | BLOCKED |
| Hashes match | BLOCKED | BLOCKED |

---

## Baseline model scores

_Run command (per model):_
```bash
# OpenAI-compatible
OPENAI_API_KEY=sk-... npx tsx packages/eval/adapters/openai.ts \
  --items eval-set-free-er-eval-v1.jsonl --model <MODEL> --out model-answers-<MODEL>.jsonl
npx tsx packages/eval/score.ts --items eval-set-free-er-eval-v1.jsonl \
  --answers model-answers-<MODEL>.jsonl --model <MODEL> --out results-<MODEL>.json
```

_Paste actual scores here after running. If any API key is absent from env,
note it as BLOCKED._

| Model | as_of_status | transition_attribution | reversal_awareness | negative_control | Overall | Mis-calibrated? |
|-------|-------------|----------------------|-------------------|-----------------|---------|-----------------|
| gpt-4o | BLOCKED | BLOCKED | BLOCKED | BLOCKED | BLOCKED | — |
| claude-sonnet-5 | BLOCKED | BLOCKED | BLOCKED | BLOCKED | BLOCKED | — |
| claude-opus-4-8 | BLOCKED | BLOCKED | BLOCKED | BLOCKED | BLOCKED | — |

> **Release gate:** If any type scores >95% or <5% across all models, that type
> is mis-calibrated — do not release until revised. Owner sign-off required.

---

## Temporal confusion matrix

_Generated by scorer after baseline runs. Paste `temporal_confusion` array from
`results-<MODEL>.json` here._

---

## Validation outputs

### Leakage screen

```
# Paste from: build-stats-free.txt grep "[leakage]"
BLOCKED — requires live build run
```

### Ambiguity screen

```
# Paste from: build-stats-free.txt grep "[ambiguity]" or item count difference
BLOCKED — requires live build run
```

### Negative-control collision check

```
# Paste from: build-stats-free.txt grep "[negcontrol]"
BLOCKED — requires live build run
```

### Human-review checklist

Generated at: `docs/human-review-checklist-free.md` (100 items, seed 42).

- [ ] Owner has reviewed all 100 checklist items and signed off.

---

## Open questions (from design note)

| ID | Question | Status |
|----|----------|--------|
| OQ-1 | Does gold sourcing require `Source.humanReviewed = true` or only `sourceId IS NOT NULL`? | **OPEN — owner decision needed** |
| OQ-2 | Is TF-IDF offline similarity sufficient for collision check? | Accepted for v1; upgrade if pool < 1000 negatives |
| OQ-3 | Case-study claims proxy: `humanReviewed = true AND ingestedBy = 'manual'`? | **OPEN — owner decision needed** |
